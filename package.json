{
  "name": "mailsuitekiller",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Chrome extension that detects email tracking pixels in Gmail and warns before you open a tracked message.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.0.287",
    "ajv": "^8.20.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
